-- A single-line comment is one that starts with two dashes
--CREATE DATABASE MyDatabase
--GO -- Used to indicate that the above code should run as a "batch"
--DROP DATABASE MyDatabase
--GO

-- [Ctrl] + r toggles the messages/results window
USE MyDatabase -- Change the execution context for the script that follows
GO -- Run as a batch
